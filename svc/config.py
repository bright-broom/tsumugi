"""
ブランド設定 ── 別ブランド・別エリアに振り替えるときはこのファイルだけ編集する。

【重要】以下の値は仮です。エリアが確定してから差し替えてください。
        # テーマ。コンバージョンLPは campaign（緑）、プロダクト面は main（青）
PROFILE = "campaign"

PLACEHOLDER = True の間は、全ページ上部に「準備中」の帯が出ます。
"""

# テーマ。コンバージョンLPは campaign（緑）、プロダクト面は main（青）
PROFILE = "campaign"

PLACEHOLDER = True          # 本番公開時に False にする

# ── ブランド ────────────────────────────────
BRAND = "紬"
BRAND_SHORT = "紬"
BRAND_READING = "つむぎ"   # 一文字屋号なので、ロゴとtitleに読みを添える
AREA = "全国"                        # 対応範囲。地名で絞らない
SERVICE_NOTE = "打ち合わせはオンラインと電話。全国どこでもお受けします。"
DOMAIN = "example.jp"                # 仮。取得後に差し替え

# ── 連絡先（仮の値。必ず差し替える） ──────────────
TEL = "000-0000-0000"
TEL_LINK = "0000000000"              # tel: リンク用（ハイフンなし）
TEL_HOURS = "平日 9:00〜18:00"
EMAIL = "info@example.jp"
LINE_URL = ""                        # LINE公式アカウントのURL。未設定なら導線を出さない
FORM_ENDPOINT = ""                   # Formspree等のPOST先。未設定なら送信ボタンを無効化

# ── 事業者情報（構造化データと特商法表記に使う） ──────
LEGAL_NAME = "紬"   # 開業届・登記の名称が決まったら差し替える
ADDRESS_REGION = "（都道府県）"   # 事業者の所在地。対応エリアではない
ADDRESS_CITY = "（市区町村）"
ADDRESS_STREET = "（番地）"
POSTAL_CODE = "000-0000"

# ── 2人のプロフィール ─────────────────────────
MEMBERS = [
    {
        "role": "設計・実装",
        "name": "（名前）",
        "bio": "フリーランスのエンジニア。医療・建設・製造・クリエイター領域でのシステム開発とDX支援。"
               "Microsoft Azure のエンタープライズ窓口で法人サポートの経験。AZ-900 / PL-900。",
    },
    {
        "role": "撮影・取材・集客支援",
        "name": "（名前）",
        "bio": "（役割に合わせて書き換える。撮影／原稿の聞き取り／広告運用のどれを担うかを明記する）",
    },
]

# ── 応答の約束（書いたら必ず守る。守れない約束は書かない） ──
RESPONSE_PROMISE = "1営業日以内"

# ── 返答時間の実績（/about に載せる。測ってから書く） ───
RESPONSE_ACTUAL = None      # 例: "直近30件の平均 4時間12分"

# ── LCP実測値（verify.py が更新する。手で書かない） ──────
LCP_MEASURED = "0.20秒（全21ページの最大値・実測）"
