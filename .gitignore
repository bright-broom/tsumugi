.DS_Store
node_modules/
.next/
out/
.locale-build/
.artifacts/
# 社内ツールの実データ（顧客・見込み客・売上）。公開リポジトリに入れない（ADR 0036）
.data/
.vercel/
# 編集環境ごとの設定（開発サーバーの起動定義など）。納品物にも引渡し資料にも入れない
.claude/
coverage/
.env*
!.env.example
next-env.d.ts
*.tsbuildinfo
public/theme.css
public/robots.txt
public/sitemap.xml
public/images/onokoro-hero.svg

# Legacy output names must never become tracked again.
shot-*.png
verify-report*.json

# Temporary LibreOffice document locks.
.~lock.*#
