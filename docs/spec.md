# 標準仕様の検査（納品の条件）

> 最新の検査（2026-09-17・全プランページ追加）：本番全項目 **PASS 600 / WARN 3 / FAIL 0**、静的プレビュー **PASS 332 / WARN 1 / FAIL 0**、対象なし 42。固定ページ 22、Vitest 530 件、料金モデル 12 件。ページ追加による検査対象の増加で、しきい値の緩和はない（[ADR 0062](architecture/0062-plan-catalog.md)）。

> 自社サイトの公開：2026-09-17 のオーナー指示により [ADR 0056](architecture/0056-owner-authorized-publication.md) を適用。専門家確認・受入確認の未実施分は WARN に残す。顧客テンプレートでは `OWNER_PUBLICATION=null` に戻し、従来の納品条件を適用する。紬は Vercel URL・メール受付で公開し、独自ドメインやフォームの導入を前提にしない。

**仕様の正本はコード**（`tools/verify/`、`npm run verify`）。この文書はその一覧で、ずれていたらコードに合わせて直す。

> 文中のコードのパス（`src/…` `tools/scripts/…` `tools/verify/` `src/styles/` `public/` `out/`）は リポジトリルートからの相対。

---

## 検証している項目

`verify` が見ているもの。**1件でもFAILがあれば `exit 1` になります。**

### 静的検証（ブラウザ不要）

- 全ページに `tel:` リンクがあり、番号が**文字**で入っている（画像の電話番号は不可）。ヘッダーの中にも `tel:` リンクがある
- 営業時間・住所が文字で入っている
- `lang="ja"` / viewport / `h1` がちょうど1つ
- JSON-LD が valid で、`@type` `name` `address` `telephone` が揃っている
- `FAQPage` が混入していない
- 全 `<img>` に `alt` 属性がある。**先頭画像に `loading="lazy"` が付いていない**（画像の無いページは「対象なし（N/A）」として記録し、PASS に数えない）
- 実行時JSが無い（`<script>` もインラインイベントハンドラも0件）
- 内部リンクがすべて解決する
- CSSカスタムプロパティが全部定義されている（未定義参照の検出）
- フォームの `<label for>` と入力の `id` が一対一で対応している
- 料金ページに全プランの金額が載っている
- 業種別ページが4本ある（ページの有無だけ。電話とフォームのどちらを主役にするかは人の確認）
- **`design.tokens.json` と `tokens.css` が同期している**（ずれていたら納品しない）
- **採用した寸法・色15項目が出ている**（ヘッダーはADR 0006の88/72px、315×57、260×48、1104px、720px、45/24px、アイボリー `#FFF8ED`、エスプレッソ `#302820`、琥珀 `#FFB000`（ADR 0014）、角丸…）
- **使わないと決めた値が混入していない**（`#858A95`、旧Shipporiフォント、外部フォントの読み込み。屋号の端末明朝はADR 0006で採用）
- **全ページに og:image があり、その画像が out に実在する**（真っ白な共有カードを出さない）
- **ファビコンと apple-touch-icon が入っている**
- 公開条件（[ADR 0024](architecture/0024-publication-gates.md)）。既定のプレビューは不足を WARN 1 件にまとめ、`--mode production`（Vercel の本番配備では自動）は `PLACEHOLDER`・ドメイン・電話・メール・郵便番号・住所・事業者名・担当者の名前と紹介の仮の値、空の `FORM_ENDPOINT`、契約文面・事業者表示の承認記録、仕様20項目の人の確認記録の不足を条件ごとに FAIL
- 仕様20項目と検査の対応表（[ADR 0026](architecture/0026-acceptance-mapping.md)）。対応漏れ・存在しない検査名・ページに出す確認の方法との食い違いを FAIL
- title / description の長さ、ページ容量

### ブラウザ実測（Playwright + Chromium）

- **LCP 2.5秒以内**（全ページ・`PerformanceObserver` の実測値）
- **横スクロールが発生しない**（幅390px＝ガイドの検証寸法）── 自前で `overflow-x` を持つ器は除外
- **タップ領域 44px以上**（幅390px・本文中のインラインリンクは除外。WCAG AA の最小は24pxで、44pxは本ガイドの推奨値）
- **コントラスト比 WCAG AA**（34箇所。フォント実サイズと太さから大文字判定を分ける）
- **図の色が良／悪で区別できるか**（同色になると帯や印の意味が消える）
- **図のコントラスト比 WCAG AA**（全ページ。SVGの `<text>` は `color` ではなく `fill` と
  `opacity` で色が決まるので、本文用の検査では捕まらない。合成してから最悪値を出す）
- コンソールエラーが無い
- スクリーンショット出力（`shot-desktop.png` / `shot-mobile.png` / `shot-price.png`）

### 仕様20項目の確かめ方

`spec.html` の20項目は、すべてを自動検査で証明できるわけではない。項目ごとの方法は `tools/verify/acceptance.ts` の対応表が正本で、ページにも同じ方法を表示する。

- 自動検査だけ：13 表示速度、14 先頭画像
- 自動検査と人の確認：1 電話、2 営業時間、3 住所・地図、4 料金、5 電話とフォームの主役、7 写真、12 スマホとPCの内容、15 スコアを追わない、16 タップ領域、17 構造化データ
- 人の確認：6 対応エリア、8 スタッフ紹介、9 実績、10 お客様の声、11 料金・選び方の説明
- 外部サービスでの確認：18 Googleビジネスプロフィール、19 通知2系統、20 口コミの運用

人の確認・外部接続の確認は `src/content/acceptance.ts` に記録し、本番モードでは記録の無い項目を FAIL にする。レポートの `acceptance` に項目ごとの状況が残る。

### 検証で実際に見つかった不具合

この仕組みを作った初回で、**目視では気づきにくい3件**が出ました。

1. **ヘッダーの電話番号が赤地に赤で描画されていた。**
   `.tel .num` が、表のバツ印用に定義した `.no{color: 琥珀}` を拾っていたCSSのカスケード衝突。
   → コントラスト検査に `.tel .num` を追加して、機械で捕まえられるようにしました。
2. **13ページで `<h1>` が0個だった。**
   セクション見出しが全部 `<h2>` だったため。
   → ページの先頭の見出しを `h1` にすることを不変条件にして（`<Section h1>`）、`verify` が検査します。
3. **1ページだけタップ領域の判定が誤検出していた。**
   CSS適用前に計測していた競合状態。
   → スタイルシートの適用完了を待ってから計測するようにしました。
4. **図の琥珀色の注記が、エスプレッソ紺の節の上で 2.94:1 しか出ていなかった。**
   SVGの `<text>` は `fill` で色が決まるため、本文用のコントラスト検査を素通りしていました。
   → 図専用の検査を足し、`section.dark .fig` でトークンを明るい側に振り替えました。

### 文言・ルート移行後の合格件数（2026-09-14）

全項目は **PASS 577 / WARN 1 / FAIL 0**、静的検査は **PASS 359 / WARN 1 / FAIL 0**。電話の補助ラベルを削除したため、コントラストの実在する検査対象が111件から107件になった。検査条件は変更していない。型・依存・文言のチェックと112件の単体テストも通す。

### 暫定ヒーロー配置後の合格件数（2026-09-14）

全項目は **PASS 580 / WARN 1 / FAIL 0**、静的検査は **PASS 360 / WARN 1 / FAIL 0**。画像の優先読み込みが1件、新しいHTMLコピーのコントラストが2件増えた。既存の見出しのコントラスト対象も維持している。HTML属性名の大文字・小文字の両方を受け付け、Reactが出力する `fetchPriority` を正しく判定する。型・依存・文言のチェックと112件の単体テストも通す。

### 共通ヘッダー刷新後の合格件数（2026-09-14）

全項目は **PASS 592 / WARN 1 / FAIL 0**、静的検査は **PASS 360 / WARN 1 / FAIL 0**。問い合わせ・欧文屋号・メニュー操作のコントラストが各4ページ、計12件増加した。PC電話は1440px以上で表示されるため、別途の幅1440pxの操作確認で検査する。既存の21ページ検査と112件の単体テストも合格。幅320・390・768・1024・1280・1440pxで、実行時JSを無効にしたメニュー開閉、キーボード遷移、44px以上の操作領域、横はみ出しなしを確認する。

### ホーム本文刷新後の合格件数（2026-09-14）

全項目は **PASS 593 / WARN 1 / FAIL 0**、静的検査は **PASS 360 / WARN 1 / FAIL 0**。新しいレイアウトでは実在するコントラスト検査対象が1件増えた。型・lint・単体112件・全21ページのJS 0バイトも確認。変更前後のホームの全テキストと4つの図解HTMLを比較して欠落なしを確認し、本文アイコンは31個から45個に増えた。幅320・390・768・820・1024・1440pxで横はみ出しなし、アンカーのヘッダーへの隠れなし、料金切替、キーボードによるFAQ開閉を確認した。変更対象外の19ページのHTMLは同一で、`works.html` は次のビルドで検査件数だけ更新される。

### 共通フッター刷新後の合格件数（2026-09-14）

全項目は **PASS 610 / WARN 1 / FAIL 0**、静的検査は **PASS 360 / WARN 1 / FAIL 0**。問い合わせ・電話・ナビ・業種・規約・先頭移動をコントラスト検査に加え、旧電話表示の対象を含む実在要素の増減を反映した。型・lint・単体112件・全21ページのJS 0バイトを確認。幅320・390・768・1024・1440pxで横はみ出しなし、全フッターリンク44px以上、コントラスト、キーボードでの先頭移動、ページ遷移と現在地を確認した。

### Tailwind中央管理への移行後（2026-09-14・現在）

全項目は **PASS 608 / WARN 1 / FAIL 0**、静的検査は **PASS 360 / WARN 1 / FAIL 0**。型・lint・単体120件・全21ページのJS 0バイトを確認。21ページ×幅320・390・768・1440pxの計算済みスタイルと配置が移行前に一致する。JS無効でのメニュー、料金切替、FAQ、先頭への移動も確認した。

コントラスト測定をCSS・フォントの読込後に統一した。読込前に非表示要素を数える競合を修正し、移行前後とも137対象で値が一致することを確認したため合計を608件に更新した。検査項目は削除していない。色・書体・寸法はTailwindの共通テーマ、全ページのCSS入口は `src/styles/globals.css` とし、静的なインラインスタイルの再導入は `npm run check` で拒否する（[ADR 0009](architecture/0009-global-tailwind.md)）。

## 2026-09-14 新料金体系の反映

全項目 **PASS 603 / WARN 1 / FAIL 0**、静的 **PASS 358 / WARN 1 / FAIL 0**。分割切替の撤去等で実在する対象が5件減った。検査条件は緩和していない。単体122件・経営モデル9件が合格。幅320・390・1440pxで21ページの横はみ出し・旧料金混入なし、実行時JS 0。価格・作業枠のモデルとの一致、外部費を含む比較、受付準備中表示を検査する（ADR 0011）。

## 2026-09-14 情報設計の整理

所有の4項目、FAQ30問、主要ページの3グループを共通化。FAQカテゴリと質問のIDを追加した。単体113件には、質問とナビゲーションの欠落・重複、共有回答、料金と権利条件の保持を検査する6件を含む。標準603 PASS / WARN 1 / FAIL 0、静的358 PASSで検査件数・しきい値は維持。全21ページ×3画面幅の追加確認と変更範囲の一致は [ADR 0017](architecture/0017-information-architecture.md) に記録した。

### 2026-09-14：共通部品とページの表示整理

現在の総合検査は **PASS 602 / WARN 1 / FAIL 0**、静的は **PASS 358 / WARN 1 / FAIL 0**。相談案内の装飾番号をアイコン付きリストに変えたことで、実在する `.stat .k` のコントラスト対象が1件減った（134→133）。検査コードやしきい値は変更していない。判断と画面確認は [ADR 0020](architecture/0020-natural-components.md) に記録する。

### 2026-09-16 店舗テンプレート修復後の検査基準

現在の基準は全項目 **PASS 573 / WARN 1 / FAIL 0**、静的 **PASS 319 / WARN 1 / FAIL 0**（対象なし 40）。過去の 602 / 358 件からの変更は、画像のないページを対象なしとする分類など、統合済みの検査改訂による。今回しきい値は変更していない。Vitest 472 件・料金モデル 10 件が合格し、全 21 ページの実行時 JavaScript は 0。店舗テンプレートの表示・掲載許可・CTA の検証は [ADR 0048](architecture/0048-storefront-template.md) を参照。

## メール受付への変更（2026-09-16）

現在は全項目 **PASS 572 / WARN 1 / FAIL 0**、静的 **PASS 318 / WARN 1 / FAIL 0**（対象なし 40）。自社サイトの入力フォームを非表示にしたため、フォームのラベル対応検査の対象が 1 件減った。メールアドレス・フォーム非表示・受付方法別の公開条件は単体テストで検証する（ADR 0051）。

## ドメイン取得先の案内（2026-09-17）

現在は全項目 **PASS 573 / WARN 1 / FAIL 0**、静的 **PASS 318 / WARN 1 / FAIL 0**（対象なし 40）。取得先案内への導線追加に伴い、ブラウザ検査の対象が 1 件増加。料金ページに 3 社の公式 URL と取得条件を掲載。

## 2026-09-17：図解の Lucide 統一

全項目 **PASS 572 / WARN 2 / FAIL 0**、静的 **PASS 317 / WARN 2 / FAIL 0**（対象なし 40）。Vitest 482 件・料金モデル 12 件。図解 6 種類に装飾アイコンを加え、外側の説明・金額・期間・割合を保持。5 ページ × 幅 320 / 390 / 768 / 1024 / 1440 px で重なり・はみ出しを確認。TOP の HTML が 109.9 KiB になり、既存の容量目安 100 KiB に対する PASS が 1 件 WARN へ変わった。検査の基準は変更せず、経緯と制約を [ADR 0052](architecture/0052-lucide-diagrams.md) に記録する。

## 2026-09-17：飲食店向け費用比較の配置変更

全項目 **PASS 575 / WARN 1 / FAIL 0**、静的 **PASS 318 / WARN 1 / FAIL 0**（対象なし 40）。Vitest 483 件・料金モデル 12 件。TOP から飲食店ページへ料金表・予約手数料・図解・注記を移し、他業種への混入を回帰検査。幅 1440 / 390 / 320 px で確認。TOP は 95.3 KiB になり、100 KiB の容量検査を満たした。検査基準は変更していない（[ADR 0053](architecture/0053-restaurant-cost-comparison.md)）。

### 2026-09-17 事業者情報と本番公開条件

全項目 **PASS 575 / WARN 1 / FAIL 0**、静的プレビュー **PASS 318 / WARN 1 / FAIL 0**。Vitest 484 件・料金モデル 12 件。本番静的は **PASS 331 / WARN 0 / FAIL 4**。指定された正式氏名・住所・Vercel URL・紹介文を反映し、契約文面を下書きとして整備した。確認記録の不足を検知する公開条件は維持する（[ADR 0054](architecture/0054-business-publication-details.md)）。

### 2026-09-17：自社公開の承認

本番全項目 **PASS 586 / WARN 3 / FAIL 0**、静的プレビュー **PASS 318 / WARN 1 / FAIL 0**。Vitest 516 件・料金モデル 12 件。準備中の帯の解除でブラウザの検査対象が 4 件減少し、本番条件を個別に集計するためプレビューとは件数が異なる。WARN は未実施の専門家確認 2 件と受入確認記録。検査の完了と人の確認を区別する（[ADR 0056](architecture/0056-owner-authorized-publication.md)）。

### 2026-09-17：TOP の情報順序とレイアウト

本番全項目 **PASS 581 / WARN 3 / FAIL 0**、静的プレビュー **PASS 318 / WARN 1 / FAIL 0**。Vitest 516 件・料金モデル 12 件。重複セクションの整理により、ブラウザで実在要素を測る検査対象が 5 件減少した。検査コード・しきい値は変更していない。幅 320 / 390 / 768 / 1024 / 1440 px で横はみ出し、ヒーロー内の文字、ページ内リンク、キーボード開閉、メール導線を確認。実行時 JS は全 21 ページで 0（[ADR 0057](architecture/0057-home-layout.md)）。

### 2026-09-17：全ページのレイアウトと導線

本番全項目 **PASS 581 / WARN 3 / FAIL 0**、静的プレビュー **PASS 318 / WARN 1 / FAIL 0**。Vitest 518 件・料金モデル 12 件。全 21 ページ × 5 画面幅の計 105 組み合わせで横はみ出し・文字サイズ・目次リンク・表のスクロール・キーボード開閉を検査。最後の調整対象 3 ページ × 5 画面幅も再確認した。実行時 JS 0・図解数・公開条件と検査のしきい値を維持（[ADR 0058](architecture/0058-interior-page-layout.md)）。
