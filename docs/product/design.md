# デザイン

> 文中のコードのパス（`src/…` `tools/scripts/…` `tools/verify/` `src/styles/` `public/` `out/`）は リポジトリルートからの相対。

---

## デザインの出所（NOT A HOTEL デザインガイドライン v2.0）

外観・寸法・部品の契約は、**このガイドラインに準拠しています。**
基本の観測値と推奨値をトークンにしています。ヘッダーは利用者の「モダンで少し和風」という指定に合わせ、[ADR 0006](../architecture/0006-japanese-modern-header.md) のブランド仕様に更新しています。

### 採った値（ガイドの章番号つき）

| 項目 | 値 | 出所 |
|---|---|---|
| 白い面／暗い章／CTAパネル／終端 | `#FFFFFF` / `#0B0B0D` / `#1C1C1C` / `#000000` | 05 色とテーマ |
| 補助面／入力面／線 | `#F4F5F7` / `#F7F7F7` / `#E9E9EB` | 05 |
| キャンペーンの緑／プロダクトの青 | `#42D083` / `#0171E3` | 05 |
| 補助文字 | `#626873`（**観測値 `#858A95` は採らない**） | 05 |
| ヘッダー高 | PC 88px → モバイル 72px | ADR 0006（07 寸法を更新） |
| 外側の余白 | PC 48px／ヒーロー 80px／モバイル本文 24px・ヘッダー 20px | 07 |
| 器 | 1104px・3列・間隔 32px。本文の読み幅 720px | 07 |
| 角丸 | media 8 / card 12 / panel 16 / pill 999 | 07 |
| 影 | 標準はなし | 07 |
| 主要CTA | PC 315 × 57 → モバイル 260 × 48 | 07 |
| ヒーローの日本語 | PC 45/67.5px → モバイル 24/36px | 06 文字 |
| 本文・入力 | 16px・行高 1.8 | 06 |
| 日本語の書体 | `YakuHanJP, "Hiragino Sans", Meiryo, …`（端末の書体） | 06 |
| 欧文ディスプレイ | League Gothic（OFL・自ホスティング）・幅補正 .9643 | 06 |
| ページの切り替え | 768px。カードは配置領域 960/520px で 4/2/1 列 | 07 |
| 操作への反応／パネル／初回出現 | 160ms / 280ms / 600ms | 11 |


### 紬のブランドヘッダー

生成り `#F7F5F0` に藍墨 `#19343D` を置き、真鍮色 `#85704B` の細い罫線を添えます。屋号だけを端末の明朝にし、欧文とナビゲーションは既存のゴシックにします。ヘッダーは画像に重ねず、その直上に独立した面として置きます。

PCは88px、768px以下は72px。999px以下では横並びの主ナビを全体メニューに集約し、問い合わせは常時表示します。1440px以上では電話もヘッダーに表示し、それ以下では全体メニューと既存の下部導線から利用できます。電話はアイコンと番号だけです。準備中の注意帯は残します。

メニューはネイティブの `details` / `summary`。Enter・Spaceで開閉し、Tabでリンクを移動できます。開くと二本線が閉じる記号になり、ラベルも切り替わります。端末の動きを減らす設定ではアニメーションを抑えます。色・書体・高さはトークン、文言は日本語カタログ、行き先はルート登録を正本にします。

### ガイドに従って**変えた**ところ

- **緑のボタンは暗い文字にしました。** 実測の緑＋白文字は約 1.98:1 で、通常文字に必要な 4.5:1 を
  下回ります（05 に明記）。`--color-campaign-accent-ink` を `#0B0B0D` にしています。
- **初期版では明朝と朱色をやめました。** ガイドの書体スタックは端末の日本語ゴシックで、大きな面は無彩色、
  装飾色は行動のためだけに残す（原則E）ためです。結果として**Webフォントの読み込みが 0 になり、
  LCP が 0.21 秒 → 0.30 秒（初期版・外部遮断なしの実測）になり、「フォントの自ホスティング」の宿題も消えました。** 現在は屋号に限って端末の明朝を使います。追加のフォント取得はありません。
- **業務状態の色をブランド色から分けました。** 図の「解約するとサイトは消えます」は緑ではなく
  注意の色（`--color-alert`）です。赤・黄・緑の状態はブランド色と意味を分離する（原則E）。

### テーマの振り替え

ガイド 16 のとおり、ブランド色は**キャンペーン（緑）かプロダクト（青）のどちらか**に決めます。
`src/content/config.ts` の `PROFILE` を変えるだけで全ページが切り替わります。

```ts
export const PROFILE = 'campaign' as const;   // 緑。コンバージョンLP向け（このサイトの既定）
export const PROFILE = 'main' as const;       // 青。プロダクト面向け
```

### 部品と C 番号

ガイド 08〜10 の C01–C24 に対応させています。**操作・ARIA・例外状態は移植先で満たす契約**であって、
原サイトの実装を写したものではありません。

| C 番号 | このサイトでの実装 |
|---|---|
| C01 BrandHeader | `Header.tsx` / `.hdr`。明朝の屋号、4本の主ナビ、問い合わせCTA、全体メニュー。88px / 72px |
| C02 NavigationPanel | `.menu`。全幅で使える `<details>`。2列の全体ナビと電話番号を表示（JavaScript不要） |
| C03 OutcomeHero | `.hero`。写真がないので無彩色の面＋短い価値＋主要CTA |
| C04 DisplayTitle | `.display` / `ol.steps` の連番。**短い欧文だけ**に使う |
| C05 SectionHeading | `.sh`（ラベル・見出し・導入） |
| C07/C08 Catalog/ScenarioCard | `.card`。列数は配置領域の幅で決まる |
| C14 SegmentedTabs | `.seg`。ラジオ＋`:has()`。JavaScript不要 |
| C15 Disclosure | `.acc`。`<details>` / `<summary>` |
| C16/C17 Primary/SecondaryAction | `.btn-1` / `.btn-2` |
| C18 ContextualCTA | `.fixbar`。本文・フッターを覆わないよう余白を確保 |
| C19 ComparisonMatrix | `.tbl`。`th scope` 付き、横スクロールは領域内に閉じる |
| C20 FieldGroup | `.field`。入力16px、`:has([aria-invalid])` でエラー表示 |
| C22 OrganizationFacts | `.ledger` |
| C24 Footer | `Footer.tsx` / `.ftr`。明るい面、32個のアイコン、ブランド文字、用途別案内（ADR 0008） |

### ガイドに**書かれていないこと**は、原サイトの仕様として扱いません

入力・エラー・読み込み・権限・空状態・キーボード操作は、ガイド自身が「別途設計する」と
書いている領域です（01・13）。このサイトでは `verify` が満たすべき条件を定義しています。

### アイコンは「道しるべ」にだけ使う

アイコンは**語を置き換えたときだけ**読む量を減らします。見出しの横に添えても文字列は結局読まれるので、
解読するものが1つ増えるだけです。そこでこの2種類に限定しています。

**① すでに学習済みの記号（語を置き換える）**
電話・✓／✗・警告。これ以外の「意味を持つ記号」は作りません。

**② 章の目印（6＋4個の閉じた組）**
`price` `unlimited` `cost-cut` `subsidy` `spec` `works` の6ページに固有の字を1つずつ割り当て、
**ナビ・章ラベル・カード見出しで必ず同じ字を使います**（`<Section navKey="price.html">`）。
業種4つも同様に、`.inds` の中だけで使う閉じた組です。

繰り返すから記号として学習され、ページを開いた瞬間に行き先が分かる。
1項目ごとに散らすのとは別物で、散らすと「AI感」に戻ります。**それ以外の場所には付けません。**

Lucide React（`lucide-react` 1.45.0, ISC）を**ビルド時に SVG へ変換**します。
`src/lib/icons.ts` には使用する 66 種を明示的に import しています。実行時の読み込みはゼロです。

アイコンを増やすときは公式 React コンポーネントを `ICONS` に登録し、`<Icon name="…" />` で呼びます。
表や図の HTML 文字列に必要な場合は `ic()` が同じコンポーネントを静的に描画します。
SVG パスを外部サイトから手作業でコピーする必要はありません。

`verify` が**すべてのインラインSVGに `aria-hidden="true"` か `role="img"` が付いているか**を
検査します（装飾アイコンは読み上げない、図は読み上げる、のどちらかに必ず倒すため）。

---

## スマホ表示

**ここを主戦場として設計している。** 営業の主経路が「電話 → URLを送る」で、
送った先はほぼスマートフォンだから。PCの見た目を縮めたものにはしない。

### 測ってから直す
毎回この順で回している。推測で触らない。

```
390px で（1）横スクロールする領域（2）14px未満の文字（3）アイコンと文字の比 を数える
→ 直す → 数え直す
```

| 指標 | 前 | 後 |
|---|---|---|
| 横に引かないと見えない領域 | 21 | **5**（4列の比較表だけ） |
| 12px未満の文字 | 300箇所超 | **0** |
| アイコンと文字の比 | 0.88〜1.20 倍とばらつき | **1.15 / 1.25 の2種類だけ** |
| 図の中の最小文字（実寸） | 8px（722の図を480pxに縮小） | **12.1px** |

（2）と（3）は `verify` の **27 文字の下限** と **28 アイコンと文字の比** に入れた。
数値を戻したら納品前に落ちる。

### アイコンは px ではなく em で決める
同じ `.ic-sm` が 12.5px の章ラベルにも 17px の見出しにも入るので、
px で固定すると場所ごとに比が変わり、行の中で浮いて見えていた。
`1.15em` / `1.25em` にすると比は常に一定になる。

線の太さも見た目で合わせている。小さく描くほど線は細く見えるので、
**描画後の線幅が約1.5pxに揃うよう** `stroke-width` を逆に振る（小さい方を太く）。

### 文字の下限を上げる（色は濃くしない）
補助文字 `#626873` は白地で 5.8:1 あり、読みにくさの原因は明度差ではなく**字面の小ささ**だった。
ガイド06の寸法はPC向けなので、768px以下では `--text-meta` ほか十数か所を引き上げている。
根拠はタップ領域44pxと同じ（読み手が50〜60代であるという前提）。

白（#FFFFFF）と補助面（#F4F5F7）の差は屋外の画面では見分けがつかないので、
**色を濃くする代わりに章の境目へ1pxの線**を入れて区切りを出している。

### 横に引かせない
| もの | 前 | 後 |
|---|---|---|
| 2〜3列の表 | 横スクロール＋見出し固定 | **1行ずつ縦に開く。**列名は `data-h` から各値の頭に出す |
| 4列以上の表 | 同上 | そのまま（縦に開くと1行が長くなりすぎる） |
| 表の脚注 | 器の中にいて画面外で切れていた | 器の外に出して折り返す |
| 全部の図 | 722〜776 の viewBox を 480px に縮小＝文字が8px | **縦に組み直した専用SVGに差し替え（`.fw`/`.fn`）** |
| 制作の流れ（4列10行） | 横スクロール | **縦の手順表（`.flow`）に作り替え** |

時間軸の図（`after_two_years` / `subsidy_timeline`）も例外にしなかった。
「横であることに意味がある」より「**読めない軸よりは読める並び**」を採った。

### 指の下でのふるまい
JavaScript は0バイトのまま、CSSだけで。

| やったこと | なぜ |
|---|---|
| `touch-action: manipulation` | ダブルタップ判定の待ち時間（約300ms）を消す |
| `:active` で 0.97 倍・120ms | **アプリらしさの大半はここ。**色ではなく反応の速さで決まる |
| `-webkit-tap-highlight-color: transparent` | 端末まかせの灰色い矩形を、上の `:active` に置き換える |
| `scroll-behavior: smooth` ＋ `scroll-padding-top` | ページ内リンクが固定ヘッダーの下に潜らない |
| `overscroll-behavior: contain` | 表を引き切った勢いがページに渡って「戻る」に化けるのを止める |
| `@view-transition { navigation: auto }` | ページ遷移が一拍つながる。ヘッダーと下部CTAは `view-transition-name` で動かさない |
| スクロール駆動アニメーションの進捗バー | 読み進みの目安。`@supports` で囲ってあるので非対応なら出ないだけ |

動きを減らす設定の人には、上の全部を切っている（`prefers-reduced-motion`）。

**やらなかったこと：`content-visibility: auto`。**
LCPが実測0.30秒で描画が詰まっていないのに、
スクロール中の高さ推定のずれ（バーが跳ねる）というリスクだけ増えるため。

360 / 390 / 430px に加え 320 / 768px でも横あふれ0を確認している。

---

## 図（`src/content/diagrams.ts`）

散文でしか説明できていなかった「仕組み」だけを図にしました。**飾りの図は作りません。**

| 図 | 置き換えたもの | 使っているページ |
|---|---|---|
| `land_vs_own()` | **主張の中心の視覚版。**借地と所有を左右に並べ、解約した日に手元に何が残るかを出す。**建物（作るもの）は左右でわざと同じ見た目**にしてあり、違うのは土地の名義と結果だけ | index / owned |
| `rent_vs_own()` | 「掲載料は借りている場所の家賃です」の段落 | index |
| `money_flow()` | 掲載料→運用費の振替の説明 | cost-cut |
| `after_two_years()` | 2年後の総額比較テーブルの金額行 | index / price |
| `subsidy_bar()` | 補助金の計算の内訳（`calc` と `ledger`） | index / subsidy |
| `subsidy_timeline()` | 補助金の手順6段（`ol.steps`）。**交付決定の前に着手すると全額対象外**という順序の制約が要点なので、そこだけを赤い線で示す | subsidy |

作り方の決めごと：

- **色は2つだけ意味を持つ。**`--fig-accent`（こちらが勧める側）と `--fig-ok`（残るもの）。
  `--fig-bad`（失われるもの）を加えた3つ。ほかは全部 `currentColor` の濃淡なので、明るい節・墨紺の節・ダークテーマのどこに置いても成立します
- **暗い章ではトークンを振り替える。**濃い色は `#0B0B0D` の上で 4.5:1 を割るので、
  `section.dark .fig` で `--fig-ok:#7FD3AC` / `--fig-bad:#FF8E84` に差し替えています
  （この振り替えが必要なことは、下に書いた図専用のコントラスト検査が見つけました）
- **`marker` の id は図ごとに固有にする。**1ページに複数の図が載るため、
  `fig()` が図の aria-label から決まる接尾辞を付けます（`dg-a…` / `dg-p…`）。
  通し番号にしないのは、ページを並列に書き出すとビルドのたびに番号が変わり、同じ入力から同じ HTML が出なくなるため
- **サイズは `viewBox` で決めて、表示幅はCSSに任せる。**ただし狭い画面では縮小も横スクロールもしない。
  **5本すべてにスマホ用の組み直し（`.fn`）を用意して差し替える。**
  縮小すると 722 の図が 480px になり、12の文字が **8px 相当**まで落ちて読めなくなるため
  （実測して分かった。当初は時間軸の図だけ横スクロールのままにしていた）

---

## 共有カード（`tools/scripts/og.ts`）

**営業の主経路は「電話 → URLを送る」で、地方ではそれが LINE になります。**
リンクを貼ったときにカードが真っ白か、屋号と金額が出るかで開封率が変わるので、
OGP画像は装飾ではなく**導線の一部**として扱っています。

- **ページごとに1枚**（15枚）。共有されたページの見出しがそのままカードに出る
- 屋号・読み・業種・3つの約束・月額を1枚に。**サイトを開かなくても要点が伝わる**
- ファビコンは屋号の一文字。SVG＋apple-touch-icon＋512px
- ビルドは `public/og/` をそのまま出すだけなので、**ビルドに Chromium を要求しません**。
  文面を変えたら `npm run og` を実行して `public/og/` の差分をコミットすること
- `verify` が「og:image があるか」「その画像が out に実在するか」を全ページで検査します

## ホーム本文のデザイン（ADR 0007）

ヒーロー以下は日本画の装飾から切り替え、白・グレー・チャコール、ゴシック、大きな数字で構成します。図解は幅を確保し、見出しと説明をPCで左右に分けます。修正費と内訳は計算と説明を並べ、料金は比較しやすいカード、FAQは罫線で整理します。スマートフォンでは一列を基本にします。

既存の4図解と説明文を残し、本文のLucideアイコンを31個から45個に増やしました。用途はページ内の道案内と修正内容の識別です。色の意味は既存の良／悪・注意と共有します。価格・条件は変更しません。CSSは `src/styles/home.css`、器の幅は `home-container` トークン、追加コピーは `i18n/locales/ja/home.ts` を正本にします。[判断の記録](../architecture/0007-home-editorial-layout.md)も参照してください。

## 共通フッターのデザイン（ADR 0008）

明るいグレーの面に切り替え、相談の見出し、アイコン付きの案内、連絡先、大きなTSUMUGIの文字で終端を構成します。全21ページで共通です。32個のLucideアイコンを配置し、狭い画面では装飾矢印4個を隠します。主要・業種・法務の20ページへの案内を保ち、現在地とページ先頭への移動にも対応します。

CSSは `src/styles/footer.css`、追加文言は `shell.footer` が正本です。既存の黒いフッター指定とホーム限定の上書きは取り除きました。[判断の記録](../architecture/0008-icon-rich-footer.md)を参照してください。


## CSSの管理と実装

Tailwind CSS 4.3.3を使い、85個の色・書体・寸法は `src/styles/design.tokens.json` から `@theme` へ生成する。公開ページの入口は `src/styles/globals.css` に統一し、各部品・ホーム・フッターの共通クラスを `@apply` で定義する。各ページはクラス名を使い、見た目の直接指定を持たない。SVGの座標は図解データ、比率などの数値はCSS変数として渡す。特殊な描画やアニメーションも同じグローバルCSSに置く。

共有カードの描画CSSも `src/styles/og.css` に移し、共通テーマから画像生成時だけコンパイルする。通常ページへの追加JavaScriptはない。実装・検証の境界は [ADR 0009](../architecture/0009-global-tailwind.md) と [site/README](../development.md) を参照。
