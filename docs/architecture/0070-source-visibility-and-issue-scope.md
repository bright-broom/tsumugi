# 0070 — ソース公開案内を実態へ合わせ、完了条件を広げずIssueを整理する

日付：2026-09-18。状態：採用。関連：#14 #37 #40、ADR 0027・0047・0056。

## 判断

Repository APIはprivateを返す。ADR0027で既に定めた切替に従い、`SOURCE_REPOSITORY_URL=null`としてworks/specを「このサイトのソースコードは、現在公開していません。」へ切り替える。自社リポジトリの可視性、顧客へのソース納品と閲覧招待の説明は変更しない。公開を再開する場合は、一般閲覧者が実際にアクセスできることを確認してURLを設定する。

AGENTS.mdの過去のPublic承認は履歴として残し、現在もPublicという誤認を訂正する。非公開へ変更した理由・実施者は推測しない。

- #14：ADR0056で承認済みVercel URLを自社公開先とし、独自ドメイン取得を公開条件から外している。旧必須条件としてはnot_plannedで終了する。将来の顧客ドメイン接続は案件別の導入確認として扱い、本課題を無期限に延長しない。
- #37：ADR0047でmacOSの既存書体を正本に採用し、生成環境・非破壊の再現コマンドを確定済み。今回も25ファイルすべてバイト一致。completedで終了する。別OS/書体への切替は将来の任意判断であり、元の再現性修正の完了を妨げない。
- #40：ローカル修正と公開先への反映を区別し、PR統合・実公開確認までopenを維持する。

## 構成と検証

共通フロントエンドガイドのREADME/GUIDELINESを確認し、既存のNote・日本語カタログと条件分岐を使用。新規部品・アイコン・ライブラリ・CSS・実行時JSの追加なし。価格変更なし。ガイドの新規候補選定は不要。

既存の非公開表示へ切り替える設定変更のため、その1行を写した単体テストは追加せず、全体検査と生成HTML・ブラウザーでの非公開表示/リンク除去・顧客納品説明の維持を検証する。結果はstatus.mdを参照。実機・専門家確認の既存WARNは残す。
